array.shift() // Tira o primeiro da posição do array
array.unshift() // Adiciona na primeira posição do array
some() // Verifica se existe pelo menos uma ave na lista.
includes() // Verifica se um valor existe dentro de um array
indexOf() // Ele procura um valor dentro de um array e retorna a posição (índice) onde ele foi encontrado. Se encontrar → retorna o número da posição Se NÃO encontrar → retorna -1
splice() // Serve para remover, adicionar ou substituir elementos no array.